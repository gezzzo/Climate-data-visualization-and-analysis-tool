version: '3.8'

services:
  climate-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./content:/app/content:ro
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    container_name: climate-visualization-app
